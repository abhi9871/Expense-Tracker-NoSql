<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <title>Reset Password</title>
    <style>
        /* Adjust the body to occupy the full viewport height */
        body {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #fceabb 0%,#f8b500 100%);
        }

        /* Add a little transparency to the card */
        .card {
            background-color: rgba(255, 255, 255, 0.8);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-4">
                <div class="card shadow-lg">
                    <div class="card-body">
                        <div class="text-center">
                            <h3><i class="bi bi-shield-lock-fill fs-1"></i></h3>
                            <h2 class="text-center">New Password</h2>
                            <p>You can enter your new password here.</p>
                            <% if (!success) { %>
                                <div class="alert alert-danger my-2 p-1" role="alert">
                                    Password must be atleast 8 characters long.
                                </div>
                            <% } %>                            
                            <form action="/password/updatepassword/<%= requestId %>" method="post" class="form" id="resetpassword-form">
                                <div class="mb-3">
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="bi bi-pencil-square"></i></span>
                                        <input id="password" name="password" placeholder="Enter new password" class="form-control" type="password" required>
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <input id="resetpassword" class="btn btn-primary btn-block w-100" type="submit" value="Reset Password">
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
</body>
</html>